<div>
  <%= simple_form_for([@company, @customer, @quote]) do |f| %>
    <%= f.input :description %>
    <%= f.input :ref_quote %>
    <%= f.input :deposit %>
    <%= f.input :price_duty_free %>
    <%= f.input :price_all_taxes %>
    <%= f.submit %>
    <%= link_to '< Back', company_customers_path(@company), class: 'btn btn-primary' %>
  <% end %>
</div>