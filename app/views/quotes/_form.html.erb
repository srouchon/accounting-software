<div>
  <%= simple_form_for([@company, @customer, @quote]) do |f| %>
    <%= f.input :ref_quote, label: 'Référence Devis:' %>
    <%= f.input :description, as: :text, label: 'Description:' %>
    <%= f.submit %>
    <%= link_to '< Retour', company_customer_quotes_path(@company, @customer), class: 'btn btn-primary' %>
  <% end %>
</div>

<!--
<div>
  <%= simple_form_for([@company, @customer, @quote]) do |f| %>
    <div class="form-group">
      <%= f.input :ref_quote, label: 'Référence Devis:' %>
    </div>
    <div class="form-group">
      <%= f.input :description, as: :text, label: 'Description:' %>
    </div>
    <div class="form-group">
      <div id='services'>
        <div class='service d-flex'>
          <%= f.simple_fields_for(:services) do |builder| %>
            <%= render 'service_fields', f: builder %>
          <% end %>
          <%= f.simple_fields_for(:quote_services) do |builder| %>
            <%= render 'quote_service_fields', f: builder %>
          <% end %>
        </div>
      </div>
    </div> 
    <div class='links'>
      <a href="#" id='btn_new_line'>Ajouter une prestation</a>
      <%= link_to_add_association 'Ajouter prestation', f, :services, class: 'btn btn-primary', id: 'btn_add_service', data: { association_insertion_node: '#services', association_insertion_method: :append } %>
      <%= link_to_add_association 'Ajouter quantité', f, :quote_services, class: 'btn btn-primary', id: 'btn_add_quote_service', data: { association_insertion_node: '#services', association_insertion_method: :append } %>
    </div>
    <div class="form-group">
      <%= f.input :price_duty_free, label: 'Prix total HT:' %>
      <%= f.input :price_all_taxes, label: 'Prix total TTC:' %>
    </div>
    <div class="form-group">
      <%= f.input :deposit, label: 'Acompte:' %>
    </div>
    <div class='links'>  
      <%= f.submit 'Valider le devis' %>
      <%= link_to '< Retour', company_customer_quotes_path(@company, @customer), class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
-->