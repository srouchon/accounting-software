<div class="service">
  <h2>Catalogue de Prestations</h2>
  <div>
    <%= link_to 'Créer une prestation', new_company_service_path(@company), class: 'btn btn-primary' %>
  </div>
  <br>
  <table border="1">
    <thead>
      <tr>
        <th>Ref_Prestation</th>
        <th>Description</th>
        <th>Prix unitaire</th>
        <th>Options</th>
      </tr>
    </thead>
    <tbody>
      <% @services.each do |service| %>
      <tr>
        <td><%= service.ref_service %></td>
        <td><%= service.description_service %></td>
        <td><%= service.unit_price %></td>
        <td>
          <%= link_to 'modifier', edit_company_service_path(@company, service), class: 'btn btn-warning' %>
          <%= link_to 'supprimer', company_service_path(@company, service), method: :delete, class: 'btn btn-danger', data: { confirm: "Supprimer?" } %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
<div>
  <%= link_to '< Retour', company_path(@company), class: 'btn btn-primary' %>
</div>
