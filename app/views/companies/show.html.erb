<div>
  <h1><%= @company.name %></h1>
  <h3>Dashboard</h3>
</div>
<div>
  <%= link_to 'Edit', edit_company_path(@company), class: 'btn btn-warning' %>
  <%= link_to 'Remove', company_path(@company), method: :delete, class: 'btn btn-danger' %>
</div>